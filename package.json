{
  "name": "study-timer",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && pnpm dev",
    "lint": "cd frontend && pnpm lint",
    "test": "cd frontend && pnpm test",
    "pb": "cd backend\\.pb-binary && pocketbase.exe serve --dir=../pb_data --migrationsDir=../pb_migrations --hooksDir=../pb_hooks"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
